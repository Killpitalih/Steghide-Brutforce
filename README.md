# Steghide-Brutforce
Finding the password for files in an image

## Using Example
python3 shpassword -w="rockyou.txt" -f="myImage.jpg" -v

- `-w` - select wordlist
- `-f` - select image
- `-v` - show all attempts
